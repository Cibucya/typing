#include <string>

std::string generate_random_phraze(const std::vector<std::string>& words, const int& end, const int& cols);
void print_phrase(const std::string& phrase, const int& phraseIndex, const std::vector<char>& colors);
std::string pickup_phrase(const std::vector<std::string>& words, const int words_num);
